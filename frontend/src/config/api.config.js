/**
 * API 配置文件
 * 包含 API 相关的所有配置信息
 */

// API基础URL配置
const apiConfig = {
  // 更新为与curl测试相同的域名
  BASE_URL: 'http://communityapi.jinshuqingci.com',
  // 或者使用相对路径，让浏览器自动使用当前域名
  // BASE_URL: '',
};

export default apiConfig; 
